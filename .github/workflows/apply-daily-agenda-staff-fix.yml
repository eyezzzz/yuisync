name: Apply agenda daily staff and catalog fix

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/apply-daily-agenda-staff-fix.yml

permissions:
  contents: write

jobs:
  apply-fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout fix branch
        uses: actions/checkout@v4
        with:
          ref: fix/agenda-daily-staff-service-catalog
          fetch-depth: 0

      - name: Apply source changes
        run: python .github/apply-agenda-daily-staff-service-catalog.py

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate
        run: |
          npm run test:transactions
          npm run typecheck
          npm run build

      - name: Publish fix branch
        run: |
          rm .github/apply-agenda-daily-staff-service-catalog.py
          rm .github/workflows/apply-daily-agenda-staff-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: agenda diaria equipe e catalogo de servicos"
          git push origin HEAD:fix/agenda-daily-staff-service-catalog

      - name: Remove temporary workflow
        run: |
          git fetch origin main
          git checkout -B main origin/main
          git rm .github/workflows/apply-daily-agenda-staff-fix.yml
          git commit -m "chore: remove agenda catalog fix executor"
          git push origin main
